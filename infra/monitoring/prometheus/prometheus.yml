global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    project: obsidian-sync

scrape_configs:
  - job_name: sync-api
    metrics_path: /api/v1/metrics
    static_configs:
      - targets:
          - host.docker.internal:3000
        labels:
          service: sync-api
